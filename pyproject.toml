[project]
name = "claude-code-mlx-proxy"
version = "0.2.0"
description = "Anthropic Messages API proxy for local MLX models via mlx-lm server"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "httpx>=0.28.0",
    "python-dotenv>=1.1.1",
]
